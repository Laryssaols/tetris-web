
const tamanho = document.getElementById("classico");

if (tamanho.classList.contains("classico")) {
    tamanho=1;
    
} else {
    // A classe não é "classico", faça outra coisa ou retorne outro valor
    console.log("A classe não é 'classico'");
    // Faça alguma outra coisa aqui, se necessário
}
